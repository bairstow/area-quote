/** @jsx jsx */
import { jsx } from '@emotion/core'

const Input = props => (
  <div
    css={{
      width: '300px',
    }}
    {...props}
  >
    Input PlaceHolder
  </div>
);

export default Input;


